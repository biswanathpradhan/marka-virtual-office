# ============================================
# Virtual Office - Environment Configuration
# ============================================
# Copy this file to .env and update the values below
# DO NOT commit .env file with real credentials to version control
# ============================================

# --------------------------------------------
# Server Configuration
# --------------------------------------------
PORT=5000
NODE_ENV=development

# --------------------------------------------
# Client URL (Frontend)
# --------------------------------------------
# Development: http://localhost:3000
# Production: https://yourdomain.com
CLIENT_URL=http://localhost:3000

# --------------------------------------------
# MySQL Database Configuration
# --------------------------------------------
# Update these values according to your MySQL setup
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=node-office

# --------------------------------------------
# JWT Authentication
# --------------------------------------------
# Generate a strong secret key with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# IMPORTANT: Change this in production!
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-long-change-in-production
JWT_EXPIRE=7d

# --------------------------------------------
# File Upload Configuration
# --------------------------------------------
# Maximum file size in bytes (25MB = 26214400 bytes)
MAX_FILE_SIZE=26214400
# Upload directory (relative to server root)
UPLOAD_DIR=./server/uploads
# Call recordings directory (relative to server root)
RECORDINGS_DIR=./server/recordings

# --------------------------------------------
# Optional: Database Connection Pool Settings
# --------------------------------------------
# These are optional and have defaults in database.js
# Uncomment and modify if needed:
# DB_CONNECTION_LIMIT=10
# DB_QUEUE_LIMIT=0

# --------------------------------------------
# Optional: Rate Limiting (configured in code)
# --------------------------------------------
# API: 100 requests per 15 minutes
# Auth: 5 requests per 15 minutes
# Upload: 10 requests per minute
# These are hardcoded in server/middleware/security.js
# Modify there if you need different limits

# ============================================
# Production Checklist:
# ============================================
# [ ] Change NODE_ENV to "production"
# [ ] Update CLIENT_URL to your production domain
# [ ] Set a strong JWT_SECRET (use crypto.randomBytes)
# [ ] Use a dedicated MySQL user (not root)
# [ ] Set a strong MySQL password
# [ ] Ensure uploads and recordings directories exist
# [ ] Set up HTTPS/SSL
# [ ] Configure firewall rules
# [ ] Set up automated backups
# ============================================

